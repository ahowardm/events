<div class="container">
    <h1>Eventos</h1>
</div>

<div class="container">
    <ul>
        <% @events.each do |event| %>
            <li><%= link_to event.name, event %> - <%= event.venue.name %></li>
        <% end %>
    </ul>
</div>