<div class="container">
    <h1>Categoría: <%=  @category.name %></h1>
</div>

<div class="container">
    <h3>Eventos de la categoría:</h3>
    <ul>
        <% @events.each do |event| %>
            <li><%= event.name %></li>
        <% end %>
    </ul>
</div>