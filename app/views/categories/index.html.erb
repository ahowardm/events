<div class="container">
    <h1>Categorías</h1>
</div>

<div class="container">
    <ul>
        <% @categories.each do |category| %>
            <li><%= category.name %></li>
        <% end %>
    </ul>
</div>