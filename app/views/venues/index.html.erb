<div class="container">
    <h1>Ubicaciones</h1>
</div>

<div class="container">
    <ul>
        <% @venues.each do |venue| %>
            <li><%= link_to venue.name, venue %></li>
        <% end %>
    </ul>
</div>